buildscript {
    repositories {
        jcenter()
    }
}

def revealJsFolder = new File(revealjspath)

System.out.println "Reveal.js directory:" + revealJsFolder.getCanonicalPath()


task export(type: Exec) {
	inputs.dir 'src/main/jade'
  inputs.dir 'src/main/css'
  commandLine './export.sh'
  args revealJsFolder.getCanonicalPath()
}
